# this file has the model structure
# Also can be converted to folder if necessary
import torch
from torch import nn

# TODO: rename the class of the model. if you change it remember to change the import in main.py


class LinearRegressionModel(nn.Module):
    def __init__(self):
        super().__init__()
        # TODO:need to add layers

    def forward(self, x):
        pass
        # TODO:need define forward
